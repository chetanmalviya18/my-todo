services:
  - name: my-todo
    type: web
    env: python
    branch: main
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app --bind 0.0.0.0:10000"
     envVars:
      - key: FLASK_ENV
        value: production
    disk:
      name: data
      sizeGB: 1